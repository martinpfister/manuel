lib.backgroundImage < temp.rootLineMedia
lib.backgroundImage {
    references.cObject.fieldName = tx_{$plugin.templatebootstrap.packageKey}_backgroundimage
    references.cObject.fieldName.insertData = 1

    # Your own rendering definition here.
    # See temp.rootLineMedia object for reference.
    # Remember: You could easily use the image properties
    # (title, description, etc.) as well to achieve any
    # level of awesomeness!

    # renderObj.2 >

}